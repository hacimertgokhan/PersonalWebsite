import Navigation from "../components/Navigation.tsx";

export default function Career() {
    return (
        <main className="bg-slate-900 w-screen h-screen flex overflow-hidden">

            <Navigation/>
        </main>
    );
}